<div class="list-ul">
  <h2 class="list-ul__heading">{{ section.settings.heading }}</h2>
   {% for block in section.blocks %}
     <ul>
       <li>{{ block.settings.text_item }}</li>
     </ul>
    {% endfor %}
</div>

{% schema %}
  {
    "name" : "List UL",
    "settings": [
      {
        "type": "text",
        "id" : "heading",
        "label": "Heading",
        "default": "this is Heading"
      },
      {
        "type": "range",
        "id": "number_column",
        "label": "Number Column",
         "min": 1,
        "max": 4,
        "step": 1,
        "default": 4
      }
    ],
    "blocks" : [
      {
        "type": "column",
        "name": "Column",
        "settings" : [ 
          {
          "type": "text",
          "id": "text_item",
          "label" : "Item",
          "default": "This is li"
        }]

      }
    ],
    "presets": [
      {
        "name": "List Ul"
      }
    ]
  }
{% endschema %}

{% stylesheet %}
{% endstylesheet %}

{% javascript %}
{% endjavascript %}