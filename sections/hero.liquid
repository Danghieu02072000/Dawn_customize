<div class="hero-infor">
  <h2 class="hero__title"> {{ section.settings.heross }}</h2>
  <h2 class="hero__heading">{{ section.settings.heading }}</h2>
  <p class="hero__desc">{{ section.settings.desc }}</p>
</div>

{% schema %}
  {
    "name" : "Herro",
    "settings": [
      {
        "type": "text",
        "id" : "heading",
        "label": "Heading Hero",
        "default": "this is Hero"
      },
      {
        "type": "text",
        "id" : "desc",
        "label": "Heading Hero",
        "default": "this is Hero"
      }
      
    ],
    "presets": [
      {
        "name": "Hero"
      }
    ]
    
  }
{% endschema %}

{% stylesheet %}
{% endstylesheet %}

{% javascript %}
{% endjavascript %}