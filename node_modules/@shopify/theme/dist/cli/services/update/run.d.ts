import { AdminSession } from '@shopify/cli-kit/node/session';
interface Options {
    script?: string;
    'from-theme': string;
    'to-theme': string;
}
export declare function run(session: AdminSession, options: Options): Promise<void>;
export {};
